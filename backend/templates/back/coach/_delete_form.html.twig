<form method="post" action="{{ path('app_back_coach_delete', {'id': coach.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coach.id) }}">
    <button class="btn">{% include './assets/delete.svg.twig' %}</button>
</form>
